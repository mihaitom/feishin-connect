# Server
PORT=9181

# Debug logging — verbose playback logs for ALL renderers (AirPlay + Sonos)
# DEBUG=true

# Token for the Connect API (an insecure default is used otherwise)
# CONNECT_TOKEN=change-me

# Pre-configure devices locally, without going through Feishin
# TARGETS=sonos:Arbeitszimmer,airplay:HomePod Kueche
